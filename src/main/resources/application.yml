server:
  port: 5060

spring:
  application:
    name: "prophius-assessment"

  devtools:
    restart:
      enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5678/prophius
    username: postgres
    password: md5d8861c5b33bc418686912b1c9512c83f
    driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl-auto: create-drop
        naming:
          implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
      show-sql: false
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQL94Dialect

  security:
    user:
      roles: admin

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info,metrics,loggers,caches, mappings


